/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package t07e15;

/**
 *
 * @author rdlrosac01
 */
public class T07E15 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        /*Quique recomienda que aquí metamos solo los atributos simples en el 
        constructor por defecto. 
        De la otra manera tenemos el vector como null. 
        DEntro de la clase película creamos el método rellenar socio, para así 
        tener el nombre y cantidad que ha aportado. Puedo llamar desde el main 
        para cada objeto de tipo película. 
        En mi método de rellenar relleno el parametrizado con los dos atributos simples y 
        luego llamo el método desde película para rellenar los socios. 
        peliculas[i].rellenarSocios();
        
        El método de rellenar socios no necesita llamar ningún vector desde fuera
        porque es un atributo de la propia clase. 
        
        El mostrar película se hace dentro de la clase película. Para ese método tiene 
        sout (n¡titulo), sout(coste) y luego un for para poder mostrar los socios (por ejemplo
        con un tostring desde socio).
        
        Para buscar el mayor en la clase principal llamo al método de calcularRentabilidad
        dentro del cual tengo un for que suma lo que abonan los socios y luego sumarlo, retorno 
        entonces suma -  precio. Dentro del main entonces tengo mi método donde asigno a la 
        variable mayor el valor de la celda cero y luego ya voy con el for y dentro un if 
        para ir actualizando la variable mayor. 
        PRimero un metodo dentro de la clase para un solo objeto y ya luego desde el main lo 
        aplico 
        
        Para buscar la película es un bucle while hasta que encuentro la película y puedo 
        rentabilizar el método de calcular rentabilidad de nuevo. 
        
        Dentro de la clase película me creo un método que compare la cantidad que abonan 
        y la que introduce el usuario para comparar. y ya en el main para todas las películas
        Y el método me retorna cuantos socios han pagado más de la cantidad e ir sumandolo.
        
        
        */
    }
    
}
